name: WPA Performance Optimization

on:
  workflow_dispatch:
    inputs:
      force_webp:
        description: Rebuild all WebP files
        required: false
        default: false
        type: boolean
  push:
    branches:
      - main
      - feature/ga4-webp-lazy-cls
    paths:
      - "**