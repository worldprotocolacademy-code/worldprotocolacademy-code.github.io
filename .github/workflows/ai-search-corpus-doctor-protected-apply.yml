name: AI Search Corpus Doctor - PROTECTED APPLY

on:
  workflow_dispatch:
    inputs:
      mode:
        description: 'Generate a plan or apply an approved plan'
        required: true
        default: PLAN_ONLY
        type: choice
        options:
          - PLAN_ONLY
          - APPLY
      approval_phrase:
        description: 'For APPLY type exactly: APPLY PROTOCOL-AI