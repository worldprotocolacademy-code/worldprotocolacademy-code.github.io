import v351 from './wpa-protocol-bot-v35.1-safe-entrypoint.mjs';
import { buildOrchestrationPlan, COMMAND_HIERARCHY, SOURCE_COMPLIANCE_GATE } from './wpa-central-orchestrator.mjs';
import { VERSION as DOCTRINE_VERSION, CANONICAL_CHAIN, REQUIRED_GATES, buildDoctrineReview } from './wpa-doctrine-kernel.mjs';

const VERSION='v35.2-connected-vessels-phase4';
const PUBLIC_ROOT='https://worldprotocolacademy.mk';
const EXTRA_SYSTEMS={
  premium_briefings:{name:'WPA Premium Briefings',url:'/wpa-briefings.html',status:'curated_human_approved_output'},services:{name:'WPA Services',url:'/wpa-services.html',status:'public_request_builder_no_commercial_activation'},institutional_profile:{name:'WPA Institutional Profile',url:'/wpa-one-page-service-profile.html',status:'institutional_public_profile'},digital_pavilion:{name:'WPA Digital Pavilion',url:'/tools/wpa-digital-pavilion/',status:'public_experience_hub'},protocolometry:{name:'WPA Protocolometry Center',url:'/protocolometry-center.html',status:'operational'},intelligence_center:{name:'WPA Protocolometry Center — legacy route',url:'/intelligence-center.html',status:'protocolometry_legacy_alias'},live_feed:{name:'WPA Public Analysis Feed',url:'/wpa-live-intelligence-feed.html',status:'public_source_candidate_feed_human_verification_required'},academic_search:{name:'WPA Academic Search Hub',url:'/tools/academic-search-hub/',status:'staging_external_discovery'},wpaws:{name:'WPAWS 17 Executive Agents',url:'/wpaws/',status:'orchestrated_executive_agent_layer'},protocol_symbols:{name:'WPA Protocol Symbols Lab',url:'/wpaws/protocol-symbols-verified/',status:'verified_dataset_ui'},multi_ai:{name:'WPA AI Council 80',url:'/multi-ai-command-center.html',status:'controlled_tactical_operational_layer'},journal_issue_1:{name:'WPA Journal — Volume I · Issue I',url:'/journal/vol-1-issue-1-2026.html',status:'public_flipbook_forthcoming_identifiers'},diplomatic_analysis:{name:'WPA Diplomatic Analysis Lab',url:'/wpaws/diplomatic-analysis-lab/',status:'phase1_public_source_analysis'}};
const ROUTES=[
{id:'protocolometry',terms:['protocolometry','протоколометрија','мерење','index','индекс'],url:'/protocolometry-center.html'},{id:'intelligence_center',terms:['intelligence center','legacy intelligence'],url:'/intelligence-center.html'},{id:'premium_briefings',terms:['premium briefings','briefing','брифинг','бриф'],url:'/wpa-briefings.html'},{id:'services',terms:['services','услуги','proposal','понуда','обука','audit'],url:'/wpa-services.html'},{id:'institutional_profile',terms:['institutional profile','институционален профил'],url:'/wpa-one-page-service-profile.html'},{id:'digital_pavilion',terms:['digital pavilion','дигитален павилјон','павилјон'],url:'/tools/wpa-digital-pavilion/'},{id:'wpa_watch',terms:['wpa watch','rss','atom','monitoring','мониторинг'],url:'/tools/wpa-watch/'},{id:'journal_watch',terms:['journal watch','editorial queue','уредничка'],url:'/journal/watch/'},{id:'journal_live',terms:['journal live','live monitor','глобален монитор'],url:'/journal/live/'},{id:'live_feed',terms:['live feed','живо следење','јавни извори'],url:'/wpa-live-intelligence-feed.html'},{id:'wpaws',terms:['wpaws','17 agents','17 агенти','агенти'],url:'/wpaws/'},{id:'academic_search',terms:['academic search hub','academic search','академско пребарување','bibliographic discovery','библиографско пребарување','сите книги','all books'],url:'/tools/academic-search-hub/'},{id:'protocol_symbols',terms:['protocol symbols','state symbols','знамиња','химни'],url:'/wpaws/protocol-symbols-verified/'},{id:'multi_ai',terms:['multi-ai','ai consensus','80 council','council 80'],url:'/multi-ai-command-center.html'},{id:'journal_issue_1',terms:['volume i issue i','wpa journal issue'],url:'/journal/vol-1-issue-1-2026.html'},{id:'diplomatic_analysis',terms:['diplomatic analysis','дипломатска анализа'],url:'/wpaws/diplomatic-analysis-lab/'}];
const clean=s=>String(s||'').normalize('NFKC').toLowerCase().replace(/[^\p{L}\p{N}]+/gu,' ').trim();
const absolute=(path,env)=>String(env?.WPA_PUBLIC_ROOT||PUBLIC_ROOT).replace(/\/+$/,'')+path;
const routeMessage=message=>{const q=clean(message);return ROUTES.filter(route=>route.terms.some(term=>q.includes(clean(term))));};
function responseHeaders(request,env){const origin=request.headers.get('Origin')||'';const allowed=['https://worldprotocolacademy-code.github.io','https://worldprotocolacademy.com','https://www.worldprotocolacademy.com',...String(env?.ALLOWED_ORIGINS||'').split(',').map(x=>x.trim()).filter(Boolean)];return {'content-type':'application/json; charset=utf-8','cache-control':'no-store','access-control-allow-origin':allowed.includes(origin)?origin:allowed[0],'access-control-allow-methods':'GET,POST,OPTIONS','access-control-allow-headers':'content-type,authorization','x-wpa-runtime':VERSION,'vary':'Origin'};}
const json=(body,request,env,status=200)=>new Response(JSON.stringify(body,null,2),{status,headers:responseHeaders(request,env)});
async function readMessage(request,url){let message=url.searchParams.get('message')||url.searchParams.get('q')||'';let context={};if(request.method==='POST'){try{const body=await request.clone().json();message=message||body.message||body.q||body.prompt||'';context=body.context||{};}catch{}}return {message:String(message).trim(),context};}
export const __test={routeMessage,EXTRA_SYSTEMS,ROUTES,buildOrchestrationPlan,CANONICAL_CHAIN,REQUIRED_GATES};

export default {async fetch(request,env){
  if(request.method==='OPTIONS')return new Response(null,{status:204,headers:responseHeaders(request,env)});const url=new URL(request.url);
  if(url.pathname==='/connected-vessels/health')return json({ok:true,version:VERSION,base:'v35.1-safe-entrypoint',routes:ROUTES.length,centralOrchestrator:true,doctrineKernel:true,claudeLive:false},request,env);
  if(url.pathname==='/connected-vessels/routes')return json({ok:true,version:VERSION,routes:ROUTES.map(r=>({...r,url:absolute(r.url,env)})),extraSystems:Object.fromEntries(Object.entries(EXTRA_SYSTEMS).map(([id,x])=>[id,{id,...x,url:absolute(x.url,env)}]))},request,env);
  if(url.pathname==='/connected-vessels/route'){const {message,context}=await readMessage(request,url);const matches=routeMessage(message);return json({ok:true,version:VERSION,message,context,matches:matches.map(r=>({...r,url:absolute(r.url,env)})),orchestration:buildOrchestrationPlan(message),human_review_required:true},request,env);}
  if(url.pathname==='/orchestrator/plan'||url.pathname==='/academic-research/plan'){const {message,context}=await readMessage(request,url);return json({ok:true,version:VERSION,message,context,plan:buildOrchestrationPlan(message,{mode:url.searchParams.get('mode')||undefined}),human_review_required:true},request,env);}
  if(url.pathname==='/doctrine-kernel/manifest')return json({ok:true,version:DOCTRINE_VERSION,authority:'sande_smiljanov',canonical_chain:CANONICAL_CHAIN,required_gates:REQUIRED_GATES,automatic_doctrine_change:false},request,env);
  if(url.pathname==='/doctrine-kernel/review'){const {message,context}=await readMessage(request,url);const plan=buildOrchestrationPlan(message,{authorialCorpusVerified:context.authorial_corpus_verified===true});return json({ok:true,version:DOCTRINE_VERSION,review:plan.doctrine_review,source_compliance_gate:plan.source_compliance_gate,release_status:plan.release_status},request,env);}
  if(url.pathname==='/orchestrator/manifest')return json({ok:true,version:VERSION,identity:'Sande commands; Doctrine Kernel protects; GPT and Claude provide strategy; Virtual Sande orchestrates.',command_hierarchy:COMMAND_HIERARCHY,source_compliance_gate:SOURCE_COMPLIANCE_GATE,systems:EXTRA_SYSTEMS,pipeline:buildOrchestrationPlan('WPA Institute').pipeline,claude_live:false},request,env);
  if(url.pathname==='/ask'){const {message,context}=await readMessage(request,url);const matches=routeMessage(message);const plan=buildOrchestrationPlan(message,{authorialCorpusVerified:context.authorial_corpus_verified===true});const routed=matches.map(r=>`${r.id}: ${absolute(r.url,env)}`).join('\n');const prefix=`WPA GOVERNED ORCHESTRATOR CONTEXT\nCommand chain: ${plan.canonical_chain}\nDoctrine checks: truth, WPA doctrine, Sande authorial DNA.\nSource Compliance: fail closed before content access; no bypass, silent download or unverified RAG.\nMission: ${plan.mission_class}; target: ${plan.performance.target_seconds??'extended verification'} seconds.\nActive executive agents: ${plan.wpaws_agents.map(a=>`${a.id}-${a.name}`).join(', ')}\nCouncil pillars: ${plan.council.selected_pillars.map(p=>p.name).join(', ')}\nRelevant systems:\n${routed||'General WPA Institute'}\nMandatory gates: Doctrine Kernel, Source Compliance, Diplomatic Protocol Core, Evidence, Safety, Sande Human Approval.\nClaude live adapter: false.\nUser context: ${JSON.stringify(context)}\n\n`;const target=new URL(request.url);target.searchParams.set('message',prefix+message);return v351.fetch(new Request(target.toString(),request),env);}
  return v351.fetch(request,env);
}};
